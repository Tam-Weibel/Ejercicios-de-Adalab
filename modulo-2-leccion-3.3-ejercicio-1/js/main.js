'use strict';


const user = 'Lucia';

if (user === 'Maria' || user === 'Lucia' ) {
    console.log(`Bienvenida, ${user}.`)
} else {
    console.log('Lo siento pero el usuario que has introducido no está registrado');
}